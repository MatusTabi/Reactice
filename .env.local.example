# Copy this file to .env.local and fill in the values.
# .env.local is gitignored and must never be committed.

# Turso (LibSQL)
TURSO_DATABASE_URL="libsql://reactise-patrik009.aws-ap-south-1.turso.io"
TURSO_AUTH_TOKEN="token"

# Auth.js
AUTH_SECRET="fb9f13ae706947f56194679f321d753d4178c5b99afd0f4ab7856d70ef712361" # On production, generate a random string for this, e.g. using `openssl rand -hex 32`
AUTH_GITHUB_ID="id"
AUTH_GITHUB_SECRET="secret"

# OpenRouter API key — get yours at https://openrouter.ai/keys
OPENROUTER_API_KEY="your-openrouter-api-key"

# Vercel Blob — create a store at vercel.com/dashboard → Storage → Blob
# Then run `vercel env pull` or paste the token manually.
BLOB_READ_WRITE_TOKEN=""
